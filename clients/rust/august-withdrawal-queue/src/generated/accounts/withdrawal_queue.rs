//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_pubkey::Pubkey;

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WithdrawalQueue {
    pub discriminator: [u8; 8],
    /// The vault this queue serves. Every instruction binds the passed vault
    /// account to this key.
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub vault_state: Pubkey,
    /// Copied from the vault at init; binds the CPI's mint accounts.
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub deposit_mint: Pubkey,
    /// Copied from the vault at init; binds the CPI's mint accounts.
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub share_mint: Pubkey,
    /// ATA of this PDA for `share_mint`. Holds every pending request's shares.
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub escrow_shares: Pubkey,
    /// ATA of this PDA for `deposit_mint`. Transit only: the payout is the balance
    /// delta across the CPI, forwarded to the recipient in the same instruction.
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub escrow_assets: Pubkey,
    /// Optional ops restriction on who may finalize any request. Zero is no
    /// queue-level restriction; `WithdrawalRequest::finalizer` still applies, and
    /// the request owner may always finalize their own. Read it through
    /// [`Self::allowed_finalizer`], or better, `WithdrawalRequest::may_finalize`.
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub finalizer_authority: Pubkey,
    /// Wait between request and eligibility, `0 ..= MAX_COOLDOWN_SECONDS`. Stamped
    /// per request at creation, so a change applies to new requests only.
    pub cooldown_seconds: u64,
    /// How long after scheduled eligibility a request may still be finalized,
    /// `0` (never expires) or `1 ..= MAX_FULFILLMENT_WINDOW_SECONDS`. Stamped per
    /// request at creation.
    pub fulfillment_window_seconds: u64,
    /// Queue-wide counter, incremented before it is stamped on a request, so the
    /// first stamp is 1 and a zero stamp marks a request that was never opened.
    /// Not a seed: request identity is owner-scoped. It orders events and pins
    /// stale instructions (`expected_sequence`).
    pub sequence: u64,
    /// Live requests. Zero is the drain gate for `release_vault`.
    pub pending_requests: u64,
    /// Sum of `shares` over live requests: escrowed exposure, not a demand
    /// forecast, since a mature request is a standing exit authorization.
    pub pending_shares: u64,
    /// `false` is drain mode: no new requests, existing ones finalize or cancel.
    /// Starts `false`; only `set_accepting_requests(true)` opens the queue.
    pub accepting_requests: bool,
    /// Canonical bump of this PDA. The vault accepts the canonical address only,
    /// so this must come from Anchor's `bump` at init, never a caller.
    pub bump: u8,
    /// Reserved. Carve new fields **out of** this array so `LEN` stays 370. A
    /// field carved later reads zero on every queue that already exists, so zero
    /// must mean "legacy behaviour" for it, as it does for every field above.
    pub padding: [u64; 16],
}

pub const WITHDRAWAL_QUEUE_DISCRIMINATOR: [u8; 8] = [54, 56, 158, 88, 232, 203, 241, 163];

impl WithdrawalQueue {
    pub const LEN: usize = 370;

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for WithdrawalQueue {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_withdrawal_queue(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<WithdrawalQueue>, std::io::Error> {
    let accounts = fetch_all_withdrawal_queue(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_withdrawal_queue(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<WithdrawalQueue>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<WithdrawalQueue>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::new(
            std::io::ErrorKind::Other,
            format!("Account not found: {}", address),
        ))?;
        let data = WithdrawalQueue::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_withdrawal_queue(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<WithdrawalQueue>, std::io::Error> {
    let accounts = fetch_all_maybe_withdrawal_queue(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_withdrawal_queue(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<WithdrawalQueue>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<WithdrawalQueue>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = WithdrawalQueue::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for WithdrawalQueue {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for WithdrawalQueue {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for WithdrawalQueue {
    fn owner() -> Pubkey {
        crate::AUGUST_WITHDRAWAL_QUEUE_ID
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for WithdrawalQueue {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for WithdrawalQueue {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
