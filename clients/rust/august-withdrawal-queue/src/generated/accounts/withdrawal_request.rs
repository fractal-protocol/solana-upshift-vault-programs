//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_pubkey::Pubkey;

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct WithdrawalRequest {
    pub discriminator: [u8; 8],
    /// The queue holding this request's shares.
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub queue: Pubkey,
    /// Who requested, pays rent, may cancel, and may always finalize.
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub owner: Pubkey,
    /// Deposit-mint token account paid at finalization. Never an escrow or the
    /// vault reserve, checked at request and again at finalize.
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub recipient_token_account: Pubkey,
    /// Who may finalize besides the owner. Zero means anyone. Read it through
    /// [`Self::may_finalize`].
    #[cfg_attr(
        feature = "serde",
        serde(with = "serde_with::As::<serde_with::DisplayFromStr>")
    )]
    pub finalizer: Pubkey,
    /// Shares held in the queue's escrow for this request.
    pub shares: u64,
    /// Owner-chosen id, unique per owner while this account exists.
    pub request_id: u64,
    /// Queue-wide ordering stamp at creation, from `WithdrawalQueue::open_request`.
    /// Quoted as `expected_sequence` by every instruction that targets this
    /// request.
    pub sequence: u64,
    /// Unix time the request was created.
    pub requested_at: i64,
    /// `requested_at + cooldown` at creation. Never changes: `expires_at` is
    /// measured from it, so expediting cannot move a deadline.
    pub scheduled_eligible_at: i64,
    /// Unix time finalization may begin. Equals `scheduled_eligible_at` unless
    /// `expedite_request` moved it earlier; it only ever moves earlier.
    pub eligible_at: i64,
    /// `scheduled_eligible_at + fulfillment_window` at creation, or `0` when the
    /// window is disabled. Read it through [`Self::is_expired`]. An expired
    /// request stays pending, and counted, until cancelled.
    pub expires_at: i64,
    /// Bump of this PDA, for `bump = request.bump` constraints. This account
    /// never signs.
    pub bump: u8,
    /// Reserved. Carve new fields **out of** this array so `LEN` stays 265.
    pub padding: [u64; 9],
}

pub const WITHDRAWAL_REQUEST_DISCRIMINATOR: [u8; 8] = [242, 88, 147, 173, 182, 62, 229, 193];

impl WithdrawalRequest {
    pub const LEN: usize = 265;

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for WithdrawalRequest {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_withdrawal_request(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::DecodedAccount<WithdrawalRequest>, std::io::Error> {
    let accounts = fetch_all_withdrawal_request(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_withdrawal_request(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::DecodedAccount<WithdrawalRequest>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<WithdrawalRequest>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::new(
            std::io::ErrorKind::Other,
            format!("Account not found: {}", address),
        ))?;
        let data = WithdrawalRequest::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_withdrawal_request(
    rpc: &solana_client::rpc_client::RpcClient,
    address: &solana_pubkey::Pubkey,
) -> Result<crate::shared::MaybeAccount<WithdrawalRequest>, std::io::Error> {
    let accounts = fetch_all_maybe_withdrawal_request(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_withdrawal_request(
    rpc: &solana_client::rpc_client::RpcClient,
    addresses: &[solana_pubkey::Pubkey],
) -> Result<Vec<crate::shared::MaybeAccount<WithdrawalRequest>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<WithdrawalRequest>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = WithdrawalRequest::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for WithdrawalRequest {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for WithdrawalRequest {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for WithdrawalRequest {
    fn owner() -> Pubkey {
        crate::AUGUST_WITHDRAWAL_QUEUE_ID
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for WithdrawalRequest {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for WithdrawalRequest {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
