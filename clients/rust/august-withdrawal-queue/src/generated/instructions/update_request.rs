//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_pubkey::Pubkey;

pub const UPDATE_REQUEST_DISCRIMINATOR: [u8; 8] = [0, 229, 201, 79, 235, 123, 130, 102];

/// Accounts.
#[derive(Debug)]
pub struct UpdateRequest {
    pub queue: solana_pubkey::Pubkey,

    pub owner: solana_pubkey::Pubkey,
    /// The owner's request; the signer must be its owner.
    pub request: solana_pubkey::Pubkey,
    /// Present to change where the request pays; validated like the original.
    pub new_recipient: Option<solana_pubkey::Pubkey>,
}

impl UpdateRequest {
    pub fn instruction(
        &self,
        args: UpdateRequestInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: UpdateRequestInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.queue, false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.owner, true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.request, false));
        if let Some(new_recipient) = self.new_recipient {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                new_recipient,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::AUGUST_WITHDRAWAL_QUEUE_ID,
                false,
            ));
        }
        accounts.extend_from_slice(remaining_accounts);
        let mut data = UpdateRequestInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::AUGUST_WITHDRAWAL_QUEUE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateRequestInstructionData {
    discriminator: [u8; 8],
}

impl UpdateRequestInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [0, 229, 201, 79, 235, 123, 130, 102],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for UpdateRequestInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct UpdateRequestInstructionArgs {
    pub expected_sequence: u64,
    pub min_assets_out: Option<u64>,
    pub finalizer: Option<Pubkey>,
}

impl UpdateRequestInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `UpdateRequest`.
///
/// ### Accounts:
///
///   0. `[]` queue
///   1. `[signer]` owner
///   2. `[writable]` request
///   3. `[optional]` new_recipient
#[derive(Clone, Debug, Default)]
pub struct UpdateRequestBuilder {
    queue: Option<solana_pubkey::Pubkey>,
    owner: Option<solana_pubkey::Pubkey>,
    request: Option<solana_pubkey::Pubkey>,
    new_recipient: Option<solana_pubkey::Pubkey>,
    expected_sequence: Option<u64>,
    min_assets_out: Option<u64>,
    finalizer: Option<Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl UpdateRequestBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn queue(&mut self, queue: solana_pubkey::Pubkey) -> &mut Self {
        self.queue = Some(queue);
        self
    }
    #[inline(always)]
    pub fn owner(&mut self, owner: solana_pubkey::Pubkey) -> &mut Self {
        self.owner = Some(owner);
        self
    }
    /// The owner's request; the signer must be its owner.
    #[inline(always)]
    pub fn request(&mut self, request: solana_pubkey::Pubkey) -> &mut Self {
        self.request = Some(request);
        self
    }
    /// `[optional account]`
    /// Present to change where the request pays; validated like the original.
    #[inline(always)]
    pub fn new_recipient(&mut self, new_recipient: Option<solana_pubkey::Pubkey>) -> &mut Self {
        self.new_recipient = new_recipient;
        self
    }
    #[inline(always)]
    pub fn expected_sequence(&mut self, expected_sequence: u64) -> &mut Self {
        self.expected_sequence = Some(expected_sequence);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn min_assets_out(&mut self, min_assets_out: u64) -> &mut Self {
        self.min_assets_out = Some(min_assets_out);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn finalizer(&mut self, finalizer: Pubkey) -> &mut Self {
        self.finalizer = Some(finalizer);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = UpdateRequest {
            queue: self.queue.expect("queue is not set"),
            owner: self.owner.expect("owner is not set"),
            request: self.request.expect("request is not set"),
            new_recipient: self.new_recipient,
        };
        let args = UpdateRequestInstructionArgs {
            expected_sequence: self
                .expected_sequence
                .clone()
                .expect("expected_sequence is not set"),
            min_assets_out: self.min_assets_out.clone(),
            finalizer: self.finalizer.clone(),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `update_request` CPI accounts.
pub struct UpdateRequestCpiAccounts<'a, 'b> {
    pub queue: &'b solana_account_info::AccountInfo<'a>,

    pub owner: &'b solana_account_info::AccountInfo<'a>,
    /// The owner's request; the signer must be its owner.
    pub request: &'b solana_account_info::AccountInfo<'a>,
    /// Present to change where the request pays; validated like the original.
    pub new_recipient: Option<&'b solana_account_info::AccountInfo<'a>>,
}

/// `update_request` CPI instruction.
pub struct UpdateRequestCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub queue: &'b solana_account_info::AccountInfo<'a>,

    pub owner: &'b solana_account_info::AccountInfo<'a>,
    /// The owner's request; the signer must be its owner.
    pub request: &'b solana_account_info::AccountInfo<'a>,
    /// Present to change where the request pays; validated like the original.
    pub new_recipient: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// The arguments for the instruction.
    pub __args: UpdateRequestInstructionArgs,
}

impl<'a, 'b> UpdateRequestCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: UpdateRequestCpiAccounts<'a, 'b>,
        args: UpdateRequestInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            queue: accounts.queue,
            owner: accounts.owner,
            request: accounts.request,
            new_recipient: accounts.new_recipient,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.queue.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.owner.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.request.key,
            false,
        ));
        if let Some(new_recipient) = self.new_recipient {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                *new_recipient.key,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::AUGUST_WITHDRAWAL_QUEUE_ID,
                false,
            ));
        }
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = UpdateRequestInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::AUGUST_WITHDRAWAL_QUEUE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.queue.clone());
        account_infos.push(self.owner.clone());
        account_infos.push(self.request.clone());
        if let Some(new_recipient) = self.new_recipient {
            account_infos.push(new_recipient.clone());
        }
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `UpdateRequest` via CPI.
///
/// ### Accounts:
///
///   0. `[]` queue
///   1. `[signer]` owner
///   2. `[writable]` request
///   3. `[optional]` new_recipient
#[derive(Clone, Debug)]
pub struct UpdateRequestCpiBuilder<'a, 'b> {
    instruction: Box<UpdateRequestCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> UpdateRequestCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(UpdateRequestCpiBuilderInstruction {
            __program: program,
            queue: None,
            owner: None,
            request: None,
            new_recipient: None,
            expected_sequence: None,
            min_assets_out: None,
            finalizer: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn queue(&mut self, queue: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.queue = Some(queue);
        self
    }
    #[inline(always)]
    pub fn owner(&mut self, owner: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.owner = Some(owner);
        self
    }
    /// The owner's request; the signer must be its owner.
    #[inline(always)]
    pub fn request(&mut self, request: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.request = Some(request);
        self
    }
    /// `[optional account]`
    /// Present to change where the request pays; validated like the original.
    #[inline(always)]
    pub fn new_recipient(
        &mut self,
        new_recipient: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.new_recipient = new_recipient;
        self
    }
    #[inline(always)]
    pub fn expected_sequence(&mut self, expected_sequence: u64) -> &mut Self {
        self.instruction.expected_sequence = Some(expected_sequence);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn min_assets_out(&mut self, min_assets_out: u64) -> &mut Self {
        self.instruction.min_assets_out = Some(min_assets_out);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn finalizer(&mut self, finalizer: Pubkey) -> &mut Self {
        self.instruction.finalizer = Some(finalizer);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = UpdateRequestInstructionArgs {
            expected_sequence: self
                .instruction
                .expected_sequence
                .clone()
                .expect("expected_sequence is not set"),
            min_assets_out: self.instruction.min_assets_out.clone(),
            finalizer: self.instruction.finalizer.clone(),
        };
        let instruction = UpdateRequestCpi {
            __program: self.instruction.__program,

            queue: self.instruction.queue.expect("queue is not set"),

            owner: self.instruction.owner.expect("owner is not set"),

            request: self.instruction.request.expect("request is not set"),

            new_recipient: self.instruction.new_recipient,
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct UpdateRequestCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    queue: Option<&'b solana_account_info::AccountInfo<'a>>,
    owner: Option<&'b solana_account_info::AccountInfo<'a>>,
    request: Option<&'b solana_account_info::AccountInfo<'a>>,
    new_recipient: Option<&'b solana_account_info::AccountInfo<'a>>,
    expected_sequence: Option<u64>,
    min_assets_out: Option<u64>,
    finalizer: Option<Pubkey>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
