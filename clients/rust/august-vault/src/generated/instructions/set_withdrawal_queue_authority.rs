//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use solana_pubkey::Pubkey;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const SET_WITHDRAWAL_QUEUE_AUTHORITY_DISCRIMINATOR: [u8; 8] = [173, 103, 89, 89, 154, 152, 141, 181];

/// Accounts.
#[derive(Debug)]
pub struct SetWithdrawalQueueAuthority {
      
              
          pub vault_state: solana_pubkey::Pubkey,
          
              
          pub deposit_mint: solana_pubkey::Pubkey,
          
              
          pub admin: solana_pubkey::Pubkey,
                /// The account at `new_authority`. It is required when that argument is
/// non-zero, and rejected when it is zero.
/// 
/// The handler requires its key to equal `new_authority`, that key to derive
/// as this vault's queue PDA under `WITHDRAWAL_QUEUE_PROGRAM_ID`, and the
/// account to be initialized and owned by that program. It is not typed as
/// the queue's state account because the queue depends on the vault for CPI,
/// so the reverse edge would be a dependency cycle.
/// 

    
              
          pub new_queue: Option<solana_pubkey::Pubkey>,
                /// The currently attached queue. It is required, and must sign, whenever a
/// queue is attached. The handler checks its key against the stored authority.
/// 
/// The type is `Signer` rather than `UncheckedAccount` plus a manual
/// `is_signer` check, because the field's type is what generates the account
/// meta. Anchor's CPI and client structs match on that type at codegen,
/// emitting a literal `true` for a `Signer` field and a literal `false` for an
/// unchecked one, and neither consults the runtime `AccountInfo`.
/// `release_vault` can therefore sign through `CpiContext::new_with_signer`
/// as-is. Had this field been unchecked, the meta would be hardcoded unsigned
/// and the CPI could not co-sign without patching it by hand.

    
              
          pub current_queue: Option<solana_pubkey::Pubkey>,
      }

impl SetWithdrawalQueueAuthority {
  pub fn instruction(&self, args: SetWithdrawalQueueAuthorityInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: SetWithdrawalQueueAuthorityInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(5+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.vault_state,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.deposit_mint,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.admin,
            true
          ));
                                                      if let Some(new_queue) = self.new_queue {
              accounts.push(solana_instruction::AccountMeta::new_readonly(
                new_queue,
                false,
              ));
            } else {
              accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::AUGUST_VAULT_ID,
                false,
              ));
            }
                                                                if let Some(current_queue) = self.current_queue {
              accounts.push(solana_instruction::AccountMeta::new_readonly(
                current_queue,
                true,
              ));
            } else {
              accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::AUGUST_VAULT_ID,
                false,
              ));
            }
                                accounts.extend_from_slice(remaining_accounts);
    let mut data = SetWithdrawalQueueAuthorityInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::AUGUST_VAULT_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct SetWithdrawalQueueAuthorityInstructionData {
            discriminator: [u8; 8],
            }

impl SetWithdrawalQueueAuthorityInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [173, 103, 89, 89, 154, 152, 141, 181],
                                }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for SetWithdrawalQueueAuthorityInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct SetWithdrawalQueueAuthorityInstructionArgs {
                  pub new_authority: Pubkey,
      }

impl SetWithdrawalQueueAuthorityInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `SetWithdrawalQueueAuthority`.
///
/// ### Accounts:
///
                ///   0. `[writable]` vault_state
          ///   1. `[]` deposit_mint
                ///   2. `[signer]` admin
                ///   3. `[optional]` new_queue
                      ///   4. `[signer, optional]` current_queue
#[derive(Clone, Debug, Default)]
pub struct SetWithdrawalQueueAuthorityBuilder {
            vault_state: Option<solana_pubkey::Pubkey>,
                deposit_mint: Option<solana_pubkey::Pubkey>,
                admin: Option<solana_pubkey::Pubkey>,
                new_queue: Option<solana_pubkey::Pubkey>,
                current_queue: Option<solana_pubkey::Pubkey>,
                        new_authority: Option<Pubkey>,
        __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SetWithdrawalQueueAuthorityBuilder {
  pub fn new() -> Self {
    Self::default()
  }
            #[inline(always)]
    pub fn vault_state(&mut self, vault_state: solana_pubkey::Pubkey) -> &mut Self {
                        self.vault_state = Some(vault_state);
                    self
    }
            #[inline(always)]
    pub fn deposit_mint(&mut self, deposit_mint: solana_pubkey::Pubkey) -> &mut Self {
                        self.deposit_mint = Some(deposit_mint);
                    self
    }
            #[inline(always)]
    pub fn admin(&mut self, admin: solana_pubkey::Pubkey) -> &mut Self {
                        self.admin = Some(admin);
                    self
    }
            /// `[optional account]`
/// The account at `new_authority`. It is required when that argument is
/// non-zero, and rejected when it is zero.
/// 
/// The handler requires its key to equal `new_authority`, that key to derive
/// as this vault's queue PDA under `WITHDRAWAL_QUEUE_PROGRAM_ID`, and the
/// account to be initialized and owned by that program. It is not typed as
/// the queue's state account because the queue depends on the vault for CPI,
/// so the reverse edge would be a dependency cycle.
/// 
#[inline(always)]
    pub fn new_queue(&mut self, new_queue: Option<solana_pubkey::Pubkey>) -> &mut Self {
                        self.new_queue = new_queue;
                    self
    }
            /// `[optional account]`
/// The currently attached queue. It is required, and must sign, whenever a
/// queue is attached. The handler checks its key against the stored authority.
/// 
/// The type is `Signer` rather than `UncheckedAccount` plus a manual
/// `is_signer` check, because the field's type is what generates the account
/// meta. Anchor's CPI and client structs match on that type at codegen,
/// emitting a literal `true` for a `Signer` field and a literal `false` for an
/// unchecked one, and neither consults the runtime `AccountInfo`.
/// `release_vault` can therefore sign through `CpiContext::new_with_signer`
/// as-is. Had this field been unchecked, the meta would be hardcoded unsigned
/// and the CPI could not co-sign without patching it by hand.
#[inline(always)]
    pub fn current_queue(&mut self, current_queue: Option<solana_pubkey::Pubkey>) -> &mut Self {
                        self.current_queue = current_queue;
                    self
    }
                    #[inline(always)]
      pub fn new_authority(&mut self, new_authority: Pubkey) -> &mut Self {
        self.new_authority = Some(new_authority);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = SetWithdrawalQueueAuthority {
                              vault_state: self.vault_state.expect("vault_state is not set"),
                                        deposit_mint: self.deposit_mint.expect("deposit_mint is not set"),
                                        admin: self.admin.expect("admin is not set"),
                                        new_queue: self.new_queue,
                                        current_queue: self.current_queue,
                      };
          let args = SetWithdrawalQueueAuthorityInstructionArgs {
                                                              new_authority: self.new_authority.clone().expect("new_authority is not set"),
                                    };
    
    accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
  }
}

  /// `set_withdrawal_queue_authority` CPI accounts.
  pub struct SetWithdrawalQueueAuthorityCpiAccounts<'a, 'b> {
          
                    
              pub vault_state: &'b solana_account_info::AccountInfo<'a>,
                
                    
              pub deposit_mint: &'b solana_account_info::AccountInfo<'a>,
                
                    
              pub admin: &'b solana_account_info::AccountInfo<'a>,
                        /// The account at `new_authority`. It is required when that argument is
/// non-zero, and rejected when it is zero.
/// 
/// The handler requires its key to equal `new_authority`, that key to derive
/// as this vault's queue PDA under `WITHDRAWAL_QUEUE_PROGRAM_ID`, and the
/// account to be initialized and owned by that program. It is not typed as
/// the queue's state account because the queue depends on the vault for CPI,
/// so the reverse edge would be a dependency cycle.
/// 

      
                    
              pub new_queue: Option<&'b solana_account_info::AccountInfo<'a>>,
                        /// The currently attached queue. It is required, and must sign, whenever a
/// queue is attached. The handler checks its key against the stored authority.
/// 
/// The type is `Signer` rather than `UncheckedAccount` plus a manual
/// `is_signer` check, because the field's type is what generates the account
/// meta. Anchor's CPI and client structs match on that type at codegen,
/// emitting a literal `true` for a `Signer` field and a literal `false` for an
/// unchecked one, and neither consults the runtime `AccountInfo`.
/// `release_vault` can therefore sign through `CpiContext::new_with_signer`
/// as-is. Had this field been unchecked, the meta would be hardcoded unsigned
/// and the CPI could not co-sign without patching it by hand.

      
                    
              pub current_queue: Option<&'b solana_account_info::AccountInfo<'a>>,
            }

/// `set_withdrawal_queue_authority` CPI instruction.
pub struct SetWithdrawalQueueAuthorityCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
      
              
          pub vault_state: &'b solana_account_info::AccountInfo<'a>,
          
              
          pub deposit_mint: &'b solana_account_info::AccountInfo<'a>,
          
              
          pub admin: &'b solana_account_info::AccountInfo<'a>,
                /// The account at `new_authority`. It is required when that argument is
/// non-zero, and rejected when it is zero.
/// 
/// The handler requires its key to equal `new_authority`, that key to derive
/// as this vault's queue PDA under `WITHDRAWAL_QUEUE_PROGRAM_ID`, and the
/// account to be initialized and owned by that program. It is not typed as
/// the queue's state account because the queue depends on the vault for CPI,
/// so the reverse edge would be a dependency cycle.
/// 

    
              
          pub new_queue: Option<&'b solana_account_info::AccountInfo<'a>>,
                /// The currently attached queue. It is required, and must sign, whenever a
/// queue is attached. The handler checks its key against the stored authority.
/// 
/// The type is `Signer` rather than `UncheckedAccount` plus a manual
/// `is_signer` check, because the field's type is what generates the account
/// meta. Anchor's CPI and client structs match on that type at codegen,
/// emitting a literal `true` for a `Signer` field and a literal `false` for an
/// unchecked one, and neither consults the runtime `AccountInfo`.
/// `release_vault` can therefore sign through `CpiContext::new_with_signer`
/// as-is. Had this field been unchecked, the meta would be hardcoded unsigned
/// and the CPI could not co-sign without patching it by hand.

    
              
          pub current_queue: Option<&'b solana_account_info::AccountInfo<'a>>,
            /// The arguments for the instruction.
    pub __args: SetWithdrawalQueueAuthorityInstructionArgs,
  }

impl<'a, 'b> SetWithdrawalQueueAuthorityCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: SetWithdrawalQueueAuthorityCpiAccounts<'a, 'b>,
              args: SetWithdrawalQueueAuthorityInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              vault_state: accounts.vault_state,
              deposit_mint: accounts.deposit_mint,
              admin: accounts.admin,
              new_queue: accounts.new_queue,
              current_queue: accounts.current_queue,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(5+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.vault_state.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.deposit_mint.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.admin.key,
            true
          ));
                                          if let Some(new_queue) = self.new_queue {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
              *new_queue.key,
              false,
            ));
          } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
              crate::AUGUST_VAULT_ID,
              false,
            ));
          }
                                          if let Some(current_queue) = self.current_queue {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
              *current_queue.key,
              true,
            ));
          } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
              crate::AUGUST_VAULT_ID,
              false,
            ));
          }
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = SetWithdrawalQueueAuthorityInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::AUGUST_VAULT_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.vault_state.clone());
                        account_infos.push(self.deposit_mint.clone());
                        account_infos.push(self.admin.clone());
                        if let Some(new_queue) = self.new_queue {
          account_infos.push(new_queue.clone());
        }
                        if let Some(current_queue) = self.current_queue {
          account_infos.push(current_queue.clone());
        }
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `SetWithdrawalQueueAuthority` via CPI.
///
/// ### Accounts:
///
                ///   0. `[writable]` vault_state
          ///   1. `[]` deposit_mint
                ///   2. `[signer]` admin
                ///   3. `[optional]` new_queue
                      ///   4. `[signer, optional]` current_queue
#[derive(Clone, Debug)]
pub struct SetWithdrawalQueueAuthorityCpiBuilder<'a, 'b> {
  instruction: Box<SetWithdrawalQueueAuthorityCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SetWithdrawalQueueAuthorityCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(SetWithdrawalQueueAuthorityCpiBuilderInstruction {
      __program: program,
              vault_state: None,
              deposit_mint: None,
              admin: None,
              new_queue: None,
              current_queue: None,
                                            new_authority: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      #[inline(always)]
    pub fn vault_state(&mut self, vault_state: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.vault_state = Some(vault_state);
                    self
    }
      #[inline(always)]
    pub fn deposit_mint(&mut self, deposit_mint: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.deposit_mint = Some(deposit_mint);
                    self
    }
      #[inline(always)]
    pub fn admin(&mut self, admin: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.admin = Some(admin);
                    self
    }
      /// `[optional account]`
/// The account at `new_authority`. It is required when that argument is
/// non-zero, and rejected when it is zero.
/// 
/// The handler requires its key to equal `new_authority`, that key to derive
/// as this vault's queue PDA under `WITHDRAWAL_QUEUE_PROGRAM_ID`, and the
/// account to be initialized and owned by that program. It is not typed as
/// the queue's state account because the queue depends on the vault for CPI,
/// so the reverse edge would be a dependency cycle.
/// 
#[inline(always)]
    pub fn new_queue(&mut self, new_queue: Option<&'b solana_account_info::AccountInfo<'a>>) -> &mut Self {
                        self.instruction.new_queue = new_queue;
                    self
    }
      /// `[optional account]`
/// The currently attached queue. It is required, and must sign, whenever a
/// queue is attached. The handler checks its key against the stored authority.
/// 
/// The type is `Signer` rather than `UncheckedAccount` plus a manual
/// `is_signer` check, because the field's type is what generates the account
/// meta. Anchor's CPI and client structs match on that type at codegen,
/// emitting a literal `true` for a `Signer` field and a literal `false` for an
/// unchecked one, and neither consults the runtime `AccountInfo`.
/// `release_vault` can therefore sign through `CpiContext::new_with_signer`
/// as-is. Had this field been unchecked, the meta would be hardcoded unsigned
/// and the CPI could not co-sign without patching it by hand.
#[inline(always)]
    pub fn current_queue(&mut self, current_queue: Option<&'b solana_account_info::AccountInfo<'a>>) -> &mut Self {
                        self.instruction.current_queue = current_queue;
                    self
    }
                    #[inline(always)]
      pub fn new_authority(&mut self, new_authority: Pubkey) -> &mut Self {
        self.instruction.new_authority = Some(new_authority);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = SetWithdrawalQueueAuthorityInstructionArgs {
                                                              new_authority: self.instruction.new_authority.clone().expect("new_authority is not set"),
                                    };
        let instruction = SetWithdrawalQueueAuthorityCpi {
        __program: self.instruction.__program,
                  
          vault_state: self.instruction.vault_state.expect("vault_state is not set"),
                  
          deposit_mint: self.instruction.deposit_mint.expect("deposit_mint is not set"),
                  
          admin: self.instruction.admin.expect("admin is not set"),
                  
          new_queue: self.instruction.new_queue,
                  
          current_queue: self.instruction.current_queue,
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct SetWithdrawalQueueAuthorityCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            vault_state: Option<&'b solana_account_info::AccountInfo<'a>>,
                deposit_mint: Option<&'b solana_account_info::AccountInfo<'a>>,
                admin: Option<&'b solana_account_info::AccountInfo<'a>>,
                new_queue: Option<&'b solana_account_info::AccountInfo<'a>>,
                current_queue: Option<&'b solana_account_info::AccountInfo<'a>>,
                        new_authority: Option<Pubkey>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

