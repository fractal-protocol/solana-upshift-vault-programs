//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use solana_pubkey::Pubkey;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;

pub const OVERRIDE_CONFIG_AUTHORITY_DISCRIMINATOR: [u8; 8] = [49, 53, 195, 205, 146, 192, 154, 236];

/// Accounts.
#[derive(Debug)]
pub struct OverrideConfigAuthority {
      
              
          pub program_config: solana_pubkey::Pubkey,
                /// Must be the program's current upgrade authority.

    
              
          pub upgrade_authority: solana_pubkey::Pubkey,
                /// This program's `ProgramData`, pinned by seeds so a caller cannot present
/// the `ProgramData` of an unrelated program they happen to control.

    
              
          pub program_data: solana_pubkey::Pubkey,
      }

impl OverrideConfigAuthority {
  pub fn instruction(&self, args: OverrideConfigAuthorityInstructionArgs) -> solana_instruction::Instruction {
    self.instruction_with_remaining_accounts(args, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::vec_init_then_push)]
  pub fn instruction_with_remaining_accounts(&self, args: OverrideConfigAuthorityInstructionArgs, remaining_accounts: &[solana_instruction::AccountMeta]) -> solana_instruction::Instruction {
    let mut accounts = Vec::with_capacity(3+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            self.program_config,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.upgrade_authority,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.program_data,
            false
          ));
                      accounts.extend_from_slice(remaining_accounts);
    let mut data = OverrideConfigAuthorityInstructionData::new().try_to_vec().unwrap();
          let mut args = args.try_to_vec().unwrap();
      data.append(&mut args);
    
    solana_instruction::Instruction {
      program_id: crate::AUGUST_VAULT_ID,
      accounts,
      data,
    }
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct OverrideConfigAuthorityInstructionData {
            discriminator: [u8; 8],
            }

impl OverrideConfigAuthorityInstructionData {
  pub fn new() -> Self {
    Self {
                        discriminator: [49, 53, 195, 205, 146, 192, 154, 236],
                                }
  }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
  }

impl Default for OverrideConfigAuthorityInstructionData {
  fn default() -> Self {
    Self::new()
  }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
 pub struct OverrideConfigAuthorityInstructionArgs {
                  pub new_authority: Pubkey,
      }

impl OverrideConfigAuthorityInstructionArgs {
  pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
    borsh::to_vec(self)
  }
}


/// Instruction builder for `OverrideConfigAuthority`.
///
/// ### Accounts:
///
                ///   0. `[writable]` program_config
                ///   1. `[signer]` upgrade_authority
          ///   2. `[]` program_data
#[derive(Clone, Debug, Default)]
pub struct OverrideConfigAuthorityBuilder {
            program_config: Option<solana_pubkey::Pubkey>,
                upgrade_authority: Option<solana_pubkey::Pubkey>,
                program_data: Option<solana_pubkey::Pubkey>,
                        new_authority: Option<Pubkey>,
        __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl OverrideConfigAuthorityBuilder {
  pub fn new() -> Self {
    Self::default()
  }
            #[inline(always)]
    pub fn program_config(&mut self, program_config: solana_pubkey::Pubkey) -> &mut Self {
                        self.program_config = Some(program_config);
                    self
    }
            /// Must be the program's current upgrade authority.
#[inline(always)]
    pub fn upgrade_authority(&mut self, upgrade_authority: solana_pubkey::Pubkey) -> &mut Self {
                        self.upgrade_authority = Some(upgrade_authority);
                    self
    }
            /// This program's `ProgramData`, pinned by seeds so a caller cannot present
/// the `ProgramData` of an unrelated program they happen to control.
#[inline(always)]
    pub fn program_data(&mut self, program_data: solana_pubkey::Pubkey) -> &mut Self {
                        self.program_data = Some(program_data);
                    self
    }
                    #[inline(always)]
      pub fn new_authority(&mut self, new_authority: Pubkey) -> &mut Self {
        self.new_authority = Some(new_authority);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
    self.__remaining_accounts.push(account);
    self
  }
  /// Add additional accounts to the instruction.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[solana_instruction::AccountMeta]) -> &mut Self {
    self.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[allow(clippy::clone_on_copy)]
  pub fn instruction(&self) -> solana_instruction::Instruction {
    let accounts = OverrideConfigAuthority {
                              program_config: self.program_config.expect("program_config is not set"),
                                        upgrade_authority: self.upgrade_authority.expect("upgrade_authority is not set"),
                                        program_data: self.program_data.expect("program_data is not set"),
                      };
          let args = OverrideConfigAuthorityInstructionArgs {
                                                              new_authority: self.new_authority.clone().expect("new_authority is not set"),
                                    };
    
    accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
  }
}

  /// `override_config_authority` CPI accounts.
  pub struct OverrideConfigAuthorityCpiAccounts<'a, 'b> {
          
                    
              pub program_config: &'b solana_account_info::AccountInfo<'a>,
                        /// Must be the program's current upgrade authority.

      
                    
              pub upgrade_authority: &'b solana_account_info::AccountInfo<'a>,
                        /// This program's `ProgramData`, pinned by seeds so a caller cannot present
/// the `ProgramData` of an unrelated program they happen to control.

      
                    
              pub program_data: &'b solana_account_info::AccountInfo<'a>,
            }

/// `override_config_authority` CPI instruction.
pub struct OverrideConfigAuthorityCpi<'a, 'b> {
  /// The program to invoke.
  pub __program: &'b solana_account_info::AccountInfo<'a>,
      
              
          pub program_config: &'b solana_account_info::AccountInfo<'a>,
                /// Must be the program's current upgrade authority.

    
              
          pub upgrade_authority: &'b solana_account_info::AccountInfo<'a>,
                /// This program's `ProgramData`, pinned by seeds so a caller cannot present
/// the `ProgramData` of an unrelated program they happen to control.

    
              
          pub program_data: &'b solana_account_info::AccountInfo<'a>,
            /// The arguments for the instruction.
    pub __args: OverrideConfigAuthorityInstructionArgs,
  }

impl<'a, 'b> OverrideConfigAuthorityCpi<'a, 'b> {
  pub fn new(
    program: &'b solana_account_info::AccountInfo<'a>,
          accounts: OverrideConfigAuthorityCpiAccounts<'a, 'b>,
              args: OverrideConfigAuthorityInstructionArgs,
      ) -> Self {
    Self {
      __program: program,
              program_config: accounts.program_config,
              upgrade_authority: accounts.upgrade_authority,
              program_data: accounts.program_data,
                    __args: args,
          }
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], &[])
  }
  #[inline(always)]
  pub fn invoke_with_remaining_accounts(&self, remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
  }
  #[inline(always)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
    self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
  }
  #[allow(clippy::arithmetic_side_effects)]
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed_with_remaining_accounts(
    &self,
    signers_seeds: &[&[&[u8]]],
    remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]
  ) -> solana_program_error::ProgramResult {
    let mut accounts = Vec::with_capacity(3+ remaining_accounts.len());
                            accounts.push(solana_instruction::AccountMeta::new(
            *self.program_config.key,
            false
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.upgrade_authority.key,
            true
          ));
                                          accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.program_data.key,
            false
          ));
                      remaining_accounts.iter().for_each(|remaining_account| {
      accounts.push(solana_instruction::AccountMeta {
          pubkey: *remaining_account.0.key,
          is_signer: remaining_account.1,
          is_writable: remaining_account.2,
      })
    });
    let mut data = OverrideConfigAuthorityInstructionData::new().try_to_vec().unwrap();
          let mut args = self.__args.try_to_vec().unwrap();
      data.append(&mut args);
    
    let instruction = solana_instruction::Instruction {
      program_id: crate::AUGUST_VAULT_ID,
      accounts,
      data,
    };
    let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
    account_infos.push(self.__program.clone());
                  account_infos.push(self.program_config.clone());
                        account_infos.push(self.upgrade_authority.clone());
                        account_infos.push(self.program_data.clone());
              remaining_accounts.iter().for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

    if signers_seeds.is_empty() {
      solana_cpi::invoke(&instruction, &account_infos)
    } else {
      solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
    }
  }
}

/// Instruction builder for `OverrideConfigAuthority` via CPI.
///
/// ### Accounts:
///
                ///   0. `[writable]` program_config
                ///   1. `[signer]` upgrade_authority
          ///   2. `[]` program_data
#[derive(Clone, Debug)]
pub struct OverrideConfigAuthorityCpiBuilder<'a, 'b> {
  instruction: Box<OverrideConfigAuthorityCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> OverrideConfigAuthorityCpiBuilder<'a, 'b> {
  pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
    let instruction = Box::new(OverrideConfigAuthorityCpiBuilderInstruction {
      __program: program,
              program_config: None,
              upgrade_authority: None,
              program_data: None,
                                            new_authority: None,
                    __remaining_accounts: Vec::new(),
    });
    Self { instruction }
  }
      #[inline(always)]
    pub fn program_config(&mut self, program_config: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.program_config = Some(program_config);
                    self
    }
      /// Must be the program's current upgrade authority.
#[inline(always)]
    pub fn upgrade_authority(&mut self, upgrade_authority: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.upgrade_authority = Some(upgrade_authority);
                    self
    }
      /// This program's `ProgramData`, pinned by seeds so a caller cannot present
/// the `ProgramData` of an unrelated program they happen to control.
#[inline(always)]
    pub fn program_data(&mut self, program_data: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
                        self.instruction.program_data = Some(program_data);
                    self
    }
                    #[inline(always)]
      pub fn new_authority(&mut self, new_authority: Pubkey) -> &mut Self {
        self.instruction.new_authority = Some(new_authority);
        self
      }
        /// Add an additional account to the instruction.
  #[inline(always)]
  pub fn add_remaining_account(&mut self, account: &'b solana_account_info::AccountInfo<'a>, is_writable: bool, is_signer: bool) -> &mut Self {
    self.instruction.__remaining_accounts.push((account, is_writable, is_signer));
    self
  }
  /// Add additional accounts to the instruction.
  ///
  /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
  /// and a `bool` indicating whether the account is a signer or not.
  #[inline(always)]
  pub fn add_remaining_accounts(&mut self, accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)]) -> &mut Self {
    self.instruction.__remaining_accounts.extend_from_slice(accounts);
    self
  }
  #[inline(always)]
  pub fn invoke(&self) -> solana_program_error::ProgramResult {
    self.invoke_signed(&[])
  }
  #[allow(clippy::clone_on_copy)]
  #[allow(clippy::vec_init_then_push)]
  pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
          let args = OverrideConfigAuthorityInstructionArgs {
                                                              new_authority: self.instruction.new_authority.clone().expect("new_authority is not set"),
                                    };
        let instruction = OverrideConfigAuthorityCpi {
        __program: self.instruction.__program,
                  
          program_config: self.instruction.program_config.expect("program_config is not set"),
                  
          upgrade_authority: self.instruction.upgrade_authority.expect("upgrade_authority is not set"),
                  
          program_data: self.instruction.program_data.expect("program_data is not set"),
                          __args: args,
            };
    instruction.invoke_signed_with_remaining_accounts(signers_seeds, &self.instruction.__remaining_accounts)
  }
}

#[derive(Clone, Debug)]
struct OverrideConfigAuthorityCpiBuilderInstruction<'a, 'b> {
  __program: &'b solana_account_info::AccountInfo<'a>,
            program_config: Option<&'b solana_account_info::AccountInfo<'a>>,
                upgrade_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
                program_data: Option<&'b solana_account_info::AccountInfo<'a>>,
                        new_authority: Option<Pubkey>,
        /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
  __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

