# Expected hashes of each program's reproducible verifiable build.
#
# Toolchain: `solana-verify build` in the pinned base image
#   solanafoundation/solana-verifiable-build@sha256:695f890e620db8c39afe5112e048599f8ee395a0cab5a2e572f30a72c6366cb4
#   (Solana v2.3.0, container Rust 1.86.0), driven by the committed Cargo.lock.
#
# Columns (whitespace-separated):
#   program     — the crate/library name passed to `--library-name`.
#   exec_sha256 — `solana-verify get-executable-hash` (trailing zero bytes
#                 trimmed). Equals `solana-verify get-program-hash` for the
#                 on-chain program, so this is the value used for on-chain
#                 verification and asserted by the `reproducible-build` CI job.
#   raw_sha256  — `shasum -a 256` of the full .so (byte-for-byte artifact identity).
#   size        — byte length of the .so.
#
# On an intentional bytecode change, update this file in the SAME PR; CI fails
# if a rebuilt hash disagrees. Reproduce locally: see VERIFY.md.
#
# Provenance: these values are not self-attesting — the authority is the
# `reproducible-build` CI job, which rebuilds from the commit under test and
# fails on any disagreement. A green run on a commit is what establishes that
# these hashes belong to that commit. That job also fails closed if this file
# yields no records to compare, so a truncated file cannot pass silently.
#
# NOTE: line numbers are compiled into the bytecode (Anchor error macros capture
# file!()/line!()), so inserting or deleting a line above any require! changes
# these hashes even with no logic change — often at an unchanged size. See the
# "Line numbers are part of the bytecode" note in VERIFY.md.
#
# program       exec_sha256                                                       raw_sha256                                                        size
august_vault    4ef31ac58e980c01e37a78b3fbe929106e55e555f67a479a7c567ae639735bc7  f6a9cd6a2019dad12f38cd01ef70af96256311123358ab1a490f80eff3b87ed7  627872
august_withdrawal_queue    541939769b2a91df0aa525fd93b471751665492b7b009487060a4572537a36a3  392accb8a09f5363bb3855e528708e7d6e8099d9fae743602699a17d5cd759b9  357144
