# Expected SHA-256 of each program's reproducible verifiable build.
#
# Toolchain: `solana-verify build` in the pinned base image
#   solanafoundation/solana-verifiable-build@sha256:695f890e620db8c39afe5112e048599f8ee395a0cab5a2e572f30a72c6366cb4
#   (Solana v2.3.0, container Rust 1.86.0), driven by the committed Cargo.lock.
#
# The `reproducible-build` CI job rebuilds each program with this toolchain and
# asserts the hash below (via `solana-verify get-executable-hash`). When a
# program's bytecode changes intentionally, update the matching hash here in the
# SAME PR — the diff makes the bytecode change explicit and reviewable, and CI
# fails if this file and the built bytes disagree.
#
# Reproduce locally: see VERIFY.md.
#
# program        sha256
august_vault     fca11d73ae5ba0635ee76964945c52ddcf78a167eb4c60317ae63df4a4e7cc1d
